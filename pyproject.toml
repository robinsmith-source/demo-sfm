[project]
name = "sfm-demo"
version = "0.1.0"
description = "Structure-from-Motion reconstruction pipeline with a web point-cloud viewer"
requires-python = ">=3.10,<3.13"
dependencies = [
    "pycolmap>=0.6",
    "numpy>=1.21",
]

[tool.uv]
package = false
